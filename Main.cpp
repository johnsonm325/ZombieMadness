#include "School.h"

#include <iostream>
#include <vector>
#include <string>
#include <ctime>

using std::cout;
using std::cin;
using std::endl;
using std::vector;
using std::string;

int main()
{
	//char choice;

	School school;
	school.playGame();
/*
	srand(size_t(time(NULL)));

	do
	{
		menu1.displayMenu();
		menu1.setChoice();

		if (menu1.getChoice() == 1)
		{
			Dungeon dungeon;

			dungeon.beginGame();
		}
	} while (menu1.getChoice() != 2);

	cout << "Shutting down game..." << endl;
	cin.get();
*/
	return 0;
}
